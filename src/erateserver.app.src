%%% -*- mode: erlang -*-

{application, erateserver, [
        {description, "HTTP interface for ERater, universal rate limiter"},
        {id, "ERateServer"},
        {vsn, "0.1"},
        {applications, [kernel, stdlib, lager, erater, cowboy]},
        {modules, []},
        {mod, {erateserver, []}},
        {registered, [erateserver]}
        ]}.
