%%% -*- mode: erlang -*-

{application, erateserver, [
        {description, "HTTP interface for ERater, universal rate limiter"},
        {id, "ERateServer"},
        {applications, [kernel, stdlib, erater, cowboy]},
        {mod, {erateserver, []}}
        ]}.
